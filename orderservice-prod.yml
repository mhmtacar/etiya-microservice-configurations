server:
  port: 8082
  data:
    mongodb:
      uri: "mongodb://localhost:27017/orderdb"
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/
